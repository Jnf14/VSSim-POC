{
  "scripts": {
    "dev": "npx vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "stats.js": "^0.17.0",
    "three": "^0.156.1"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^4.4.9"
  }
}
